# Claude Code (build output + runtime)
.claude/
.claude-state
.claude-state.*/
.claude-daemon/
CLAUDE.md

# Docs
docs/*
!docs/design/
# task.md 是项目主规划文档，必须保持 git 跟踪以防误删。
# 切勿对它执行 `git rm`/`git rm --cached`，也不要在 .gitignore 中忽略它。

# docs/plan/ 是 skill-tackle-plan 的运行时落盘区（plan-reader 契约计划文件），
# 设计本意就是 gitignore（落盘即生效，不入 git diff 视野，详见 docs/plan/README.md）。
# 测试若需要真实 plan fixture，应放在 test/fixtures/ 下跟踪，而非放本目录。

# PROGRESS.md 是 loop driver 运行时写入的进度状态文件（每次运行改写，无版本控制价值）。
# per-loop 隔离目录里的 PROGRESS.md 已由上方 .claude-state* 规则覆盖；此处只忽略仓库根的这一份。
/PROGRESS.md

# Node
node_modules/

# Coverage (H3: CI writes coverage.txt at repo root)
coverage.txt
coverage/
.nyc_output/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual Environments
venv/
env/
ENV/
.venv

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
*.log
logs/

# Local Config
.env
.local.*

# Secrets
*.key
*.pem
*.secret
credentials*
id_rsa*

# Archives
*.tar.gz
*.zip
